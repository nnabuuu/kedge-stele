node_modules/
dist/
prov.db
resume.html
.stele/
.DS_Store
*.tgz

# Per-machine `stele init` artifacts — leave .claude/commands/ and
# .claude/agents/ tracked (the stele slash commands + extract agent are part
# of the project), but everything else is local config or runtime state.
.mcp.json
.claude/settings.json
.claude/hooks/
.claude/skills/
.claude/scheduled_tasks.lock

# IDE / editor scratch
.idea/

# Personal working notes — not part of the public project
doc/

# Local snapshot scratch
snapshot/
.gstack/

# Design assets (mocks, explorations) — the canonical source of truth, kept on
# disk but not version-controlled in this repo.
design/
