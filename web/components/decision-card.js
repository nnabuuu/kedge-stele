// Decision card — D/DEF/OQ chip + type badge + state pill.
// To be implemented in Phase 2 (Projects) and Phase 3 (Project page).

export function renderDecisionCard(_decision) {
  // Stub for Phase 1 — will be implemented in Phase 2.
  return "";
}
